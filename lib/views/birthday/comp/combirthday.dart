import 'package:flutter/material.dart';

class ComponentBirthday extends StatelessWidget {
  ComponentBirthday({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
